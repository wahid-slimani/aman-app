# Local development environment template
# Copy to .env.local and fill with local credentials.

DATABASE_URL=postgresql://<DB_USER>:<DB_PASSWORD>@localhost:5432/aman
JWT_ACCESS_SECRET=<SET_LOCAL_SECRET>
JWT_REFRESH_SECRET=<SET_LOCAL_SECRET>
